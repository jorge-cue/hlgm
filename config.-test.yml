logging:
  level: INFO
  loggers:
    mx.jhcue: DEBUG

database:
  driverClass: org.h2.Driver
  user: sa
  password:
  url: jdbc:h2:mem;MODE=PostgreSQL;AUTO_RECONNECT=TRUE
  properties:
    charSet: UTF-8
    hibernate.dialect: org.hibernate.dialect.H2Dialect
  maxWaitForConnection: 1s
